#include <iostream>
#include <vector> 
using namespace std;

int main() 
{
    int t,n,sum;
    cin>>t;
    vector<int>v;
    while(t--)
    {
        cin>>n;
        v.push_back(n);
    }
    sum=v.back()+v[0];
    cout << sum << endl;
	return 0;
}
